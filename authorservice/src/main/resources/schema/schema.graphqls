type Query {
    authors(nameFilter: String): [Author]
    authorById(id: ID): Author
}

type Book @key(fields: "id") @extends {
    id: ID @external
    author: Author
}

type Author {
    id: ID
    name: String
}